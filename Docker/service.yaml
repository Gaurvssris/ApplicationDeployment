# apiVersion: v1
# kind: Service

# metadata:
#   name: myapp-service

# spec:

#   type: LoadBalancer

#   selector:
#     app: myapp

#   ports:

#   - port: 80

#     targetPort: 80

apiVersion: v1
kind: Service

metadata:
  name: devopsking-service

spec:
  type: LoadBalancer

  selector:
    app: devopsking

  ports:
  - port: 80
    targetPort: 80